import React from 'react';
import Modal from '../Modal/Modal';

const Home = () => {
    return (
        <div className='text-center'>
            <Modal></Modal>
        </div>
    );
};

export default Home;